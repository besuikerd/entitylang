entity File {
  name: String
  size: Int
  
  c = directory.a + directory.b
}

entity Directory {
  name: String
  size: Int = sum(files.size) + sum(subDirectories.size) + sum(files.directory.files.size)
  
  
  a: Int
  b: Int
  
}

relation File.directory 1 <-> * Directory.files
relation Directory.parent ? <-> * Directory.subDirectories