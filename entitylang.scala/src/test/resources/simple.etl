entity A{
  amount: Int
  weight: Float
  value: Float = sum(children.value) + amount * weight

  childAmount = children.amount + x

  x: Int
  y: Int+
  z = x + y


  a = b
  b = c
  c = d
  d = 2
}

relation A.parent 1 <-> + A.children
